import TaskFormPage from "../new";

export default TaskFormPage



// Esto es porque al recibir un ID del "Task" va a cargar el mismo componente "TaskFormPage" del archivo "new". Desde allí, pongo las condiciones para que se modifique dependiendo de si ha recibido o no un ID en su URL.